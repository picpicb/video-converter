server :
  port : 42308
  ssl:
    enabled: true
    key-store: "keystore.pkcs12"
    key-store-password: esipe
    keyStoreType: PKCS12

spring :
  data :
    mongodb :
      uri: mongodb+srv://ejmn-converter:Ejmn2019@clustermongodb-driver-xusit.azure.mongodb.net/ezipflix?retryWrites=true&w=majority
      database : ezipflix

conversion:
  messaging :
    rabbitmq :
      conversion-exchange: video-conversion-exchange
      conversion-queue: video-conversion-queue

rabbitmq-server:
  server: 172.31.249.234
  port: 5672
  credentials:
    vhost: ezip         # sudo rabbitmqctl add_vhost ezip
                        # sudo rabbitmqctl set_permissions -p ezip ezip ".*" ".*" ".*"
    username: rabbitmq      # sudo rabbitmqctl add_user ezip pize
    password: rabbitmq


